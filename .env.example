# Copy to .env.local before building for cPanel static deploy.
# NEXT_PUBLIC_* values are embedded at build time.
NEXT_PUBLIC_API_BASE_URL=https://hanumangadi.com/hanumangadi/api1

# Optional: PHP video proxy if direct playback fails
# NEXT_PUBLIC_VIDEO_PROXY_URL=https://hanumangadi.com/hanumangadi/video-proxy
